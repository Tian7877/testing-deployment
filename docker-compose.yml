services:
  web:
    build: .
    image: devops-learning-project:local
    ports:
      - "5000:5000"
    environment:
      - APP_VERSION=1.0.0
      - APP_ENV=local-compose
    restart: unless-stopped
